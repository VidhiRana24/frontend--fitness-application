// Define a model for the user's workout information
export interface WorkoutInformation {
  goal: string;
  fitnessLevel: string;
  gender: string;
  age: number;
  height: number;
  workoutFrequency: string;
  workoutLocation: string;
}
